import React from 'react';
import SearchBar from '../UI/SearchBar/SearchBar';

const albumFetcher = props => {
  return (
    <header>
      <SearchBar />
    </header>
  );
};

export default albumFetcher;
